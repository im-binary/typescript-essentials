"use strict";
let isDone = false;
isDone = true;
console.log(typeof isDone); // boolean
let isOk = true; // Boolean 타입 변수에는 원시값 true가 들어갈 수 있다.
// let isNotOk: boolean = new Boolean(true); // 래퍼객체는 boolean 타입이 아니다
